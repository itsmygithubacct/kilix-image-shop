# Changelog

## 0.2.1 — local development

- Create the local-only Apache-2.0 package and its reproducible 6/6-record
  offline build lock.
- Add the 7/7-module immutable project domain, project-v1 schema, canonical
  golden fixture, and 14/14 revision-checked command families.
- Add OD-7 integration slices 1/7 through 7/7: the engine-neutral API, guarded
  runtime, closed compiler, proxy pyramid, bounded tile scheduler, sparse mask
  worker, safe diagnostics, and qualification-carrier binder.
- Add the 6/6-module immutable project store with a 12/12-point save
  transaction, ownership lock, recovery, and previewed reachability collection.
- Add the 9/9-module bounded history and editing population, including
  transactional spill rollback and immutable undo/redo object reachability.
- Add the 5/5-module pure render-planning and composition population when the
  four OD-7 render modules are counted with their plan boundary.
- Add the 3/3-module deterministic export boundary with four-format normalized
  presets, bounded full-resolution staging, isolated codec ports, verified
  atomic image replacement, and versioned unsigned provenance sidecars.
- Add the 4/4-module zero-provider operation substrate with 2/2 admitted
  operation kinds, linearizable cancellation, provider/result joins, and fixed
  local diagnostics.
- Add the 2/2 application boundary modules and freeze the total functional
  implementation population at 40/40 modules.
- Retain 0/2 production provider adapters and 0/1 GUI toolkit selections until
  their separately owned gates open.
